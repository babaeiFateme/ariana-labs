const variants = {
    primary: "bg-primary text-white",
    secondary: "bg-secondary text-secondary-foreground",
    base: "bg-white text-secondary-foreground",
    destructive: "bg-destructive text-white",
};

const buttonStyle = {
    variants,
};

export default buttonStyle;
